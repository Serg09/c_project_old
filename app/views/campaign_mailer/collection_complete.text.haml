Campaign collection complete
:plain

Contribution collection for your campaign for "#{h @campaign.book.administrative_title}" targeting #{h number_to_currency(@campaign.target_amount, precision: 0)} by #{h @campaign.target_date.strftime('%-m/%-d/%Y')} has finished.
:plain

Contributions: #{@campaign.contributions.count}
Amount received: #{number_to_currency(@campaign.total_donated)}
Percent of goal: #{number_to_percentage(@campaign.current_progress * 100, precision: 0)}
:plain

-if @campaign.book.author.pending_fulfillments.any?
  Follow this link to fulfill items that are due to the contributors.
  =fulfillments_url
