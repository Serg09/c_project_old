#subscribe-form.floating
  .container
    .row
      .col-md-4.col-md-offset-4
        .panel.panel-primary
          .panel-heading
            Find Out More!
            %button.close{ type: 'button', data: { dismiss: 'alert', target: '#subscribe-form'} }
              %span{ 'aria-hidden' => true } &times;
              %span.sr-only Close
          .panel-body
            %p
              Our new beta test site is coming July 20th. Share your name and
              email with us to find out about updates, get info on tips and
              special events and deals, and to be one of the first to help us
              test the new site as we roll it out and keep adding new features!
            =form_for(@subscriber) do |f|
              =render 'subscribers/form_fields', f: f, subscriber: @subscriber
