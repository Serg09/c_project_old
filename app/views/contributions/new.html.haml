=content_for :page_title do
  Make a contribution!

.container.container-default{ 'ng-app' => 'Crowdscribed' }
  .row
    .col-md-8
      %h1 Contribute!
      %h2
        Support the book
        %i= @campaign.book.approved_version.title
        by
        =@campaign.book.author.full_name
        with a contribution.
      .app{ 'ng-controller' => 'ContributionController as contributionCtl', 'ng-init' => "campaignId = #{@campaign.id}" }
        .row.reward-option{ 'ng-repeat' => 'reward in rewards', id: "reward_{{reward.id}}" }
          .col-sm-2
            %button.btn.btn-lg.btn-success{ 'data-reward-id' => '{{reward.id}}', 'ng-click' => 'handleRewardButtonClick($event)' }
              {{ reward.minimum_contribution | currency : $ : 0}}
          %div{ 'ng-class' => '{"col-sm-6": reward.photo_id, "col-sm-10": !reward.photo_id}' }
            .reward-header {{ reward.working_description }}
            .reward-description
              {{ reward.working_long_description }}
          .col-sm-4{ 'ng-if' => 'reward.photo_id' }
            %img.img.img-responsive.img-rounded{ 'ng-src' => '/images/{{reward.photo_id}}' }
        .row.reward-option
          .col-sm-2
            %input.form-control{ type: 'text' }
          .col-sm-10
            .reward-header Choose your amount
    .col-md-4
      and the winner is!
      {{ selectedRewardId }}

